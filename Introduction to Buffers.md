 #Buffers

this section we were gone deeply understand about Buffers and Streams, and somehow we can say that these both are consider as backbone of `node.js`. 

when it come on data transfer Streams is responsible for it and surprisingly Streams itself uses buffers, so this way you can understand the importance of buffers

And apparently you don`t` able to make performant application if you have not good knowledge of about buffers because without it - 
- Don`t` even capture which thing makes lag your application-
- How much we are transferring data to our application-
- And you found yourself in an confusion always

If someone have good knowledge of buffers then, it can Keeps account of all the things even he can Take care of single bit

---

Arrays Buffer in `Node.js` Behind the seen uses two concepts of `JS` -
- [[Arrays Buffers in JavaScript]]
- Typed Arrays

So first we have to clarify both these Topics before we were gone jump on Buffers