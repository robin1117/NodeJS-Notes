As its name suggested that reflected which means server itself reflected what`s` the user provides as input in the frontend, which may further causes the chances the Data stolen from XSS 

As its name suggested that reflected which means server itself reflected what`s` the user provides as input in the frontend, which may further causes the chances the Data stolen from XSS 